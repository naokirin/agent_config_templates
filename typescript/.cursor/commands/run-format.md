# Run Format

Format TypeScript and JavaScript source files with Prettier.

1. Run `npm run format` or `npx prettier --write .` (or project format script) to format all configured files.
2. If the user specified a path, run Prettier on that path only.
3. Ensure no formatting-related changes are left unapplied; report any Prettier errors.
